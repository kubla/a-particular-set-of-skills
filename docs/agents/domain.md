# Domain Docs

Before exploring the codebase, read the root `CONTEXT.md` and any relevant ADRs under `docs/adr/`.

If those files do not exist, proceed without raising their absence. The domain-modeling workflows create them when useful terminology or decisions emerge.

Use vocabulary established in `CONTEXT.md`. Surface any conflict with an existing ADR explicitly.
